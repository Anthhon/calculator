operation -> expr, operator, expr ;

operator -> "+" | "-"
          | "*" | "/" 
          | "^" ;

expr -> operation 
      | grouping
      | NUMBER ;

grouping -> "(", expr, ")" 
          | "[", expr, "]"
          | "{", expr, "}" ;
